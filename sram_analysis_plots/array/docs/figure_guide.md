# Array Figure Guide

Generated by `scripts/plot_nvsim_array_figures.py` from the CSV outputs under `sram_analysis_plots/array/data/`.

## `figures/macro_latency_comparison.png`

1. Figure filename: `macro_latency_comparison.png` and matching `.pdf` saved alongside it
2. Brief description of what the figure shows: Read and write latency comparison across the Baseline, High Vt, Negative BL, and WL Underdrive macro cases.
3. Source data file or files used:
   - `sram_analysis_plots/array/data/nvsim_case_comparison.csv`
4. Key processing steps applied:
   - Loaded the macro case comparison table from the array data directory.
   - Used the NVSim-reported read and write latency columns without additional smoothing or normalization.
   - Plotted grouped bars on a shared nanosecond axis for the four report cases.
5. What the figure helps the reader understand: Shows which cases change the macro-level latency estimate and which cases reuse the same baseline array timing.
6. Any important caveats or assumptions:
   - Negative BL and WL Underdrive keep the baseline macro latency because this NVSim build does not directly model those assist waveforms.

## `figures/macro_energy_comparison.png`

1. Figure filename: `macro_energy_comparison.png` and matching `.pdf` saved alongside it
2. Brief description of what the figure shows: Read and write energy comparison across the Baseline, High Vt, Negative BL, and WL Underdrive macro cases.
3. Source data file or files used:
   - `sram_analysis_plots/array/data/nvsim_case_comparison.csv`
4. Key processing steps applied:
   - Loaded the array case comparison table from the generated CSV outputs.
   - Read the NVSim dynamic-energy columns directly in picojoules.
   - Plotted grouped read and write bars on one shared energy axis.
5. What the figure helps the reader understand: Makes it clear whether any optimization changes the macro-level dynamic energy estimate in the current NVSim-supported mapping.
6. Any important caveats or assumptions:
   - Only the High Vt case applies a supported leakage scaling. The other assist cases reuse the baseline NVSim energy estimate.

## `figures/macro_leakage_comparison.png`

1. Figure filename: `macro_leakage_comparison.png` and matching `.pdf` saved alongside it
2. Brief description of what the figure shows: Leakage power comparison across the Baseline, High Vt, Negative BL, and WL Underdrive macro cases.
3. Source data file or files used:
   - `sram_analysis_plots/array/data/nvsim_case_comparison.csv`
4. Key processing steps applied:
   - Loaded the NVSim case comparison table.
   - Read the leakage-power column directly in microwatts.
   - Used one bar per case with fixed case colors across the full figure set.
5. What the figure helps the reader understand: Highlights that High Vt is the only case that changes the macro leakage estimate in the current supported NVSim mapping.
6. Any important caveats or assumptions:
   - The High Vt leakage change comes from an `IoffScale` derived from TT hold-window supply energy, not from a full transistor-level macro extraction.

## `figures/macro_area_summary.png`

1. Figure filename: `macro_area_summary.png` and matching `.pdf` saved alongside it
2. Brief description of what the figure shows: Total macro area summary for the four reported SRAM cases.
3. Source data file or files used:
   - `sram_analysis_plots/array/data/nvsim_case_comparison.csv`
4. Key processing steps applied:
   - Loaded the macro case comparison table from the array data directory.
   - Read the total area column in square millimeters.
   - Plotted one bar per case to show that all cases share the same topology area in the current flow.
5. What the figure helps the reader understand: Shows that the current case comparison changes leakage or bitcell trends more than physical macro area.
6. Any important caveats or assumptions:
   - All four cases use the same macro topology, so identical area bars are expected unless a future flow explicitly changes the physical organization.

## `figures/bitcell_stability_summary.png`

1. Figure filename: `bitcell_stability_summary.png` and matching `.pdf` saved alongside it
2. Brief description of what the figure shows: TT bitcell stability summary comparing hold SNM, read SNM, and write noise margin where each case has supporting data.
3. Source data file or files used:
   - `sram_analysis_plots/array/data/nvsim_case_comparison.csv`
4. Key processing steps applied:
   - Loaded the case comparison CSV that already carries the TT bitcell support metrics alongside the macro results.
   - Plotted only the metrics available for each case instead of fabricating missing stability numbers.
   - Kept the same case color mapping used throughout the array figure set.
5. What the figure helps the reader understand: Provides one compact view of which cases improve or degrade the TT stability-related Cadence evidence.
6. Any important caveats or assumptions:
   - Missing bars mean that no robust TT metric was available for that case and metric family in the current dataset.

## `figures/optimization_trend_summary.png`

1. Figure filename: `optimization_trend_summary.png` and matching `.pdf` saved alongside it
2. Brief description of what the figure shows: Cadence-backed optimization trend summary using one representative TT improvement metric per optimization case.
3. Source data file or files used:
   - `sram_analysis_plots/array/data/bitcell_metrics_summary.csv`
   - `sram_analysis_plots/array/data/array_analysis_combined_summary.csv`
4. Key processing steps applied:
   - Loaded the long-form bitcell metric summary and the combined array summary tables.
   - Converted TT percentage deltas into positive improvement magnitudes for High Vt, Negative BL, and WL Underdrive.
   - Plotted one bar per optimization with shared percentage units to keep the summary compact.
5. What the figure helps the reader understand: Summarizes the most important Cadence-backed benefit of each optimization without implying unsupported macro-level assist modeling.
6. Any important caveats or assumptions:
   - The three bars represent different physical metrics: hold-window supply energy for High Vt, write delay for Negative BL, and read disturb for WL Underdrive.
